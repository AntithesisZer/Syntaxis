微积分的许多应用都需要我们对函数$f$的导数进行分析，从而推断出有关函数$f$的事实. 因为$f'(x)$表示函数$y=f(x)$的曲线在点$\left( x,f(x) \right)$的斜率，所以它说明了曲线在每一点处变化的方向. 所以，我们可以考虑通过研究$f'(x)$来得到有关$f(x)$的信息.

# $f'$对$f$的影响

观察导数可以知道一个函数在哪些区间上递增，哪些区间上递减.

> [!corollary|1] 单调性判定
> a. 如果在某个区间上有$f'(x)>0$，那么$f(x)$在这个区间上递增.
> b. 如果在某个区间上有$f'(x)<0$，那么$f(x)$在这个区间上递减.
> 
> `\begin{proof}`
> 设$x_{1}$、$x_{2}$为区间内的任意两个数. 不妨设$x_{1}<x_{2}$，根据增函数的定义，要证明$f(x_{1})<f(x_{2})$.
> 已知$f'(x)>0$，故$f$在$[x_{1},x_{2}]$上可微. 由中值定理可知，在$x_{1}$和$x_{2}$之间存在一个数$c$，使得
> $$
> f(x_{2})-f(x_{1}) = f'(c)(x_{2}-x_{1}) . \tag{1}
> $$
> 根据假设，有$f'(c)>0$，并且因为$x_{1}<x_{2}$，所以$x_{2}-x_{1}>0$. 因此，公式$1$的右边为正，故
> $$
> f(x_{2})-f(x_{1})>0 \text{，即} f(x_{1})<f(x_{2}) .
> $$
> 这说明$f$是递增的.
> 同理可证，若有$f'(x)<0$则$f(x_{1})>f(x_{2})$，说明$f$是递减的.
> `\end{proof}`

# 一阶导数判定

如果$f$在$c$处有极值，那么$c$一定是$f$的临界值，但并非所有临界值都对应极大值或极小值. 因此需要一个方法来判断$f$在一个临界点处是否有极值.

> [!corollary|2] 一阶导数判定
> 设$c$是连续函数$f$的一个临界值.
> a. 如果$f'$在$c$处由正变负，那么$f$在$c$处有一个极大值.
> b. 如果$f'$在$c$处由负变正，那么$f$在$c$处由一个极小值.
> c. 如果$f'$在$c$处的符号不发生改变，那么$f$在$c$处没有极大值或极小值.

一阶导数判定可由单调性判定得到. 例如对于极大值的判定，因为$f'$在$c$处由正变负，所以在$c$左侧$f$递增，在$c$右侧$f$递减，这说明$f$在$c$处有一个极大值.

# $f''$对$f$的影响

```tikz
\begin{document}
\begin{tikzpicture}[>=stealth, scale=1.6]

	\definecolor{curveblue}{HTML}{2A6F97}   % 1. 经典蓝 (原 themeblue，默认主曲线)
	\definecolor{curveorange}{HTML}{D96B27} % 2. 暖赤橙 (互补色，对比最明显)
	\definecolor{curveteal}{HTML}{0D9488}   % 3. 墨青色 (冷色调延伸)
	\definecolor{curvered}{HTML}{9E2A2B}    % 4. 绯红色 (高亮/重点强调曲线)
	\definecolor{curvegreen}{HTML}{386641}  % 5. 苔绿色 (自然稳重)
	\definecolor{curvepurple}{HTML}{5E548E} % 6. 暮紫色 (优雅暗色调)
	\definecolor{curveyellow}{HTML}{B47200} % 7. 赭黄色 (高亮度差异曲线)
	\definecolor{curverust}{HTML}{9C4A2F}   % 8. 赭石棕 (复古暖暗色)

	\definecolor{themeslate}{HTML}{475569} % 坐标系
	\definecolor{themegrid}{HTML}{E2E8F0}  % 网格
	\definecolor{themefill}{HTML}{E6EEF5}  % 填充
	\definecolor{themebkg}{HTML}{F4F6F8}   % 文字背景填充

	% 网格
	% \draw[step=1cm, color=themegrid, thin] (-3.5,-3.5) grid (3.5,3.5);
	% x轴
	\draw[->, color=themeslate, thick] (-0.8,0) -- (5.8,0) node[right, font=\small] {$x$};
	% y轴
	\draw[->, color=themeslate, thick] (0,-0.8) -- (0,5.8) node[above, font=\small] {$y$};
	% 原点
	\node[color=themeslate, below left, font=\scriptsize] at (0,0) {${0}$};

	\draw[domain=1:5, samples=50, color=curveblue, very thick] plot (\x,{0.25 * \x ^ 2 - 0.5 * \x +1.25}) node [pos=0.75, above left, font=\small] {$f$};

	% x轴刻度
	% \foreach \x in {-1,1}
	% \draw[color=themeslate] (\x,1pt) -- (\x,-2pt) node[below, font=\scriptsize] {$\x$};
	% y轴刻度
	% \foreach \y in {-1,1}
	% \draw[color=themeslate] (1pt,\y) -- (-2pt,\y) node[left, font=\scriptsize] {$\y$};

\end{tikzpicture}
\end{document}
```
