# 参数方程

假设$x$和$y$都是第三个变量$t$（称为参数）的函数，由方程
$$
x=f(t),y=g(t)
$$
给出，那么这两个方程称为参数方程. 每一个$t$确定一个点$(x,y)$，可以在坐标平面中画出. 点$(x,y)=\left( f(t),g(t) \right)$随$t$的变化而变化，从而描出一条曲线$C$，称为参数曲线. 事实上，参数$t$不一定表示时间，而且也可以用别的字母来表示这个参数. 但是在参数曲线的许多应用中，$t$确实表示时间，因此我们可以将$(x,y)=\left( f(t),g(t) \right)$解释为粒子在$t$时的位置.

如果消去参数方程中的参数，得到$x$和$y$的笛卡尔方程，其图像与参数方程所表示的曲线相同. 这个过程称为消参，它有助于识别参数曲线的形状，但在这个过程中丢失了一些信息. 关于$x$和$y$的方程描述了粒子沿其运动的曲线，而参数方程有额外的优势——它们告诉我们粒子在任何给定时间的位置，并指出运动的方向.

有时$t$被限制在一个特定的区间内. 例如
$$
x=t^{2}-2t,y=t+1,0\leq t\leq4 .
$$

一般地，参数方程
$$
x=f(t),y=g(t),a\leq t\leq b
$$
对应的曲线以$\left( f(a),g(a) \right)$为起点，以$\left( f(b),g(b) \right)$为终点.

考虑参数方程
$$
x=\cos t,y=\sin t, 0\leq t\leq2\pi
$$
和
$$
x=\sin2t,y=\cos2t,0\leq t\leq 2\pi .
$$
我们可以得到
$$
x^{2}+y^{2} = \cos^{2}t+\sin^{2}t = 1 和 x^{2}+y^{2} = \sin^{2}(2t)+\cos^{2}(2t) = 1 .
$$
这两个参数方程表示的曲线都是单位圆.
```tikz
\begin{document}
\begin{tikzpicture}[>=stealth, scale=1.6]

	\definecolor{curveblue}{HTML}{2A6F97}   % 1. 经典蓝 (原 themeblue，默认主曲线)
	\definecolor{curveorange}{HTML}{D96B27} % 2. 暖赤橙 (互补色，对比最明显)
	\definecolor{curveteal}{HTML}{0D9488}   % 3. 墨青色 (冷色调延伸)
	\definecolor{curvered}{HTML}{9E2A2B}    % 4. 绯红色 (高亮/重点强调曲线)
	\definecolor{curvegreen}{HTML}{386641}  % 5. 苔绿色 (自然稳重)
	\definecolor{curvepurple}{HTML}{5E548E} % 6. 暮紫色 (优雅暗色调)
	\definecolor{curveyellow}{HTML}{B47200} % 7. 赭黄色 (高亮度差异曲线)
	\definecolor{curverust}{HTML}{9C4A2F}   % 8. 赭石棕 (复古暖暗色)

	\definecolor{themeslate}{HTML}{475569} % 坐标系
	\definecolor{themegrid}{HTML}{E2E8F0}  % 网格
	\definecolor{themefill}{HTML}{E6EEF5}  % 填充
	\definecolor{themebkg}{HTML}{F4F6F8}   % 文字背景填充

	% x轴
	\draw[->, color=themeslate, thick] (-1.8,0) -- (1.8,0) node[right, font=\small] {$x$};
	% y轴
	\draw[->, color=themeslate, thick] (0,-1.8) -- (0,1.8) node[above, font=\small] {$y$};
	% 原点
	\node[color=themeslate, below left, font=\scriptsize] at (0,0) {${0}$};

	\draw[thick, color=curveblue, domain=0:360, samples=300, variable=\t] plot ({cos(\t)}, {sin(\t)});

	\filldraw[black] (1,0) circle (0.8pt) node[above right, font=\scriptsize] {$(1,0)$};
	\filldraw[black] (0,1) circle (0.8pt) node[above right, font=\scriptsize] {$(0,1)$};
	\filldraw[black] (-1,0) circle (0.8pt) node[above left, font=\scriptsize] {$(-1,0)$};
	\filldraw[black] (0,-1) circle (0.8pt) node[below right, font=\scriptsize] {$(0,-1)$};

\end{tikzpicture}
\end{document}
```
这里需要注意，曲线是点的集合，参数曲线是点沿特定方式运动的轨迹.

# 利用技术工具绘制参数曲线

# 旋轮线

$$
x=r(\theta-\sin\theta),y=r(1-\cos\theta),\theta\in\mathbb{R} .
$$

# 参数曲线族

考虑参数方程
$$
x=a+\cos t,y=s\tan t+\sin t .
$$
当$a$取不同值时，曲线呈现不同形态. 这些曲线称为蚌线.
