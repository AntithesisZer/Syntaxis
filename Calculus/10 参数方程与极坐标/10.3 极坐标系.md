在平面中选择一点，称为极点，记作$O$. 然后从$O$出发画一条射线，称为极轴. 极轴通常是水平向右的，对应笛卡尔坐标系的$x$轴正半轴.

如果$P$为平面中的任意另一点，令$r$为$O$到$P$的距离，$\theta$为极轴与线段$OP$之间的夹角（通常以弧度为单位），那么$P$就由有序对$(r,\theta)$表示，$r$、$\theta$称为$P$的极坐标. 按惯例，夹角从极轴的逆时针方向测量为正值，从顺时针方向测量则为负值. 如果$P=O$，那么$r=0$，并且对于任意$\theta$值，$(0,\theta)$均表示极点.

如果承认点$(-r,\theta)$和点$(r,\theta)$都位于过点$O$的同一条直线上，且到$O$的距离都是$\lvert r \rvert$，只是两点分别在$O$的两侧，这样就把极坐标系$(r,\theta)$推广到$r$为负值的情况. 如果$r>0$，那么点$(r,\theta)$位于与$\theta$相同的象限；如果$r<0$，那么点$(r,\theta)$位于相反的象限. 例如，$(-r,\theta)$和$(r,\theta+\pi)$表示同一个点.

在笛卡尔坐标系中每个点只有一种表示方法，但在极坐标系中每个点有多种表示方法. 事实上，因为$2\pi$表示逆时针转一圈，所以极坐标$(r,\theta)$所表示的点也可用
$$
(r,\theta+2n\pi) 和 (-r,\theta+(2n+1)\pi)
$$
来表示，其中$n\in\mathbb{Z}$.

# 极坐标与笛卡尔坐标系之间的关系

如图，极坐标系中的极点对应笛卡尔坐标系的原点，极轴与$x$轴正半轴重合.
```tikz
\begin{document}
\begin{tikzpicture}[>=stealth, scale=1.5]

	\definecolor{curveblue}{HTML}{2A6F97}   % 1. 经典蓝 (原 themeblue，默认主曲线)
	\definecolor{curveorange}{HTML}{D96B27} % 2. 暖赤橙 (互补色，对比最明显)
	\definecolor{curveteal}{HTML}{0D9488}   % 3. 墨青色 (冷色调延伸)
	\definecolor{curvered}{HTML}{9E2A2B}    % 4. 绯红色 (高亮/重点强调曲线)
	\definecolor{curvegreen}{HTML}{386641}  % 5. 苔绿色 (自然稳重)
	\definecolor{curvepurple}{HTML}{5E548E} % 6. 暮紫色 (优雅暗色调)
	\definecolor{curveyellow}{HTML}{B47200} % 7. 赭黄色 (高亮度差异曲线)
	\definecolor{curverust}{HTML}{9C4A2F}   % 8. 赭石棕 (复古暖暗色)

	\definecolor{themeslate}{HTML}{475569} % 坐标系
	\definecolor{themegrid}{HTML}{E2E8F0}  % 网格
	\definecolor{themefill}{HTML}{E6EEF5}  % 填充
	\definecolor{themebkg}{HTML}{F4F6F8}   % 文字背景填充

	% 定义坐标
	\coordinate (O) at (0,0);
	\coordinate (P) at (4.33, 2.5);
	\coordinate (Px) at (4.33, 0);

	% x轴
	\draw[->, color=themeslate, thick] (-0.5,0) -- (5.0,0) node[right, font=\small] {$x$};
	% y轴
	\draw[->, color=themeslate, thick] (0,-0.5) -- (0,3.0) node[above, font=\small] {$y$};
	% 原点
	\node[color=themeslate, below left, font=\scriptsize] at (0,0) {${0}$};

	% 极径 r (蓝色线段)
	\draw[very thick, curveblue!60] (O) -- (P)
	node[midway, above left, themeslate, font=\small] {$r$};
	% 垂直虚线/实线 y
	\draw[thick, themeslate] (P) -- (Px)
	node[midway, right, themeslate, font=\small] {$y$};
	% 底边 x 标注
	\node[below, themeslate, font=\small] at (2.165, 0) {$x$};
	% 直角符号
	\draw (Px) +(-0.25, 0) -- +(-0.25, 0.25) -- +(0, 0.25);
	% 极角 theta (红色弧线带箭头)
	\draw[->, curveteal!80, thick] (1.2, 0) arc (0:30:1.2);
	\node[themeslate, font=\small] at (1.55, 0.35) {$\theta$};
	% 点 P 及坐标标注
	\fill[themeslate] (P) circle (2pt);
	\node[above, themeslate, font=\small] at (P) {$P(r,\theta) = P(x,y)$};

\end{tikzpicture}
\end{document}
```
如果点$P$的笛卡尔坐标为$(x,y)$，极坐标为$(r,\theta)$，那么从图中可以看出$\cos\theta=\frac{x}{r}$，$\sin\theta=\frac{y}{r}$. 因此，当极坐标$(r,\theta)$已知时，可以通过
$$
x=r\cos\theta,\ y=r\sin\theta
$$
求其笛卡尔坐标$(x,y)$.
当笛卡尔坐标为$(x,y)$时，可以通过
$$
r^{2}=x^{2}+y^{2},\ \tan\theta=\frac{y}{x}
$$
求其极坐标$(r,\theta)$.

> [!example|1] 
> 用极坐标表示笛卡尔坐标为$(1,-1)$的点.
> 
> `\begin{proof}[解：]`
> 如果$r$为正值，那么
> $$
> \begin{align}
> r &= \sqrt[]{x^{2}+y^{2}} = \sqrt[]{2} , \\
> \tan\theta &= \frac{y}{x} = -1 .
> \end{align}
> $$
> 因为点$(1,-1)$位于第四象限，所以可以取$\theta=-\frac{\pi}{4}$或$\theta=\frac{7\pi}{4}$. 因此，一个可能的答案是$\left( \sqrt[]{2},-\frac{\pi}{4} \right)$，另一个是$\left( \sqrt[]{2},\frac{7\pi}{4} \right)$.
> `\end{proof}`

